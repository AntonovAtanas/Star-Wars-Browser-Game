function initialState () {
    const state = {
        milleniumFalcon: {
            height: 80,
            width: 80,
            startTop: Math.random()*600,
            startLeft: 0
        }
    }

    return state;
}